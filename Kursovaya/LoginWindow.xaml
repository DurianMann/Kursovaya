
<Window x:Class="Kursovaya.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursovaya"
        mc:Ignorable="d"
        Title="Авторизация" Height="450" Width="400"
        Style="{DynamicResource WindowStyle}">
    <Grid Margin="20">
        <StackPanel>

            <TextBlock Text="Касса" Margin="0 10 0 0" Style="{DynamicResource BigCaseStyle}"/>
            <TextBlock Text="Логин:" Margin="0 10 0 0" Style="{DynamicResource LoginStyle}"/>
            <TextBox x:Name="txtUsername" Margin="0 0 0 5" Style="{DynamicResource BaseTextBoxStyle}"/>

            <TextBlock Text="Пароль:" Margin="0 10 0 0" Style="{DynamicResource LoginStyle}"/>
            <PasswordBox x:Name="txtPassword" Margin="0 0 0 5" Style="{DynamicResource BasePasswordBoxStyle}"/>

            <Button Content="Войти" Click="Login_Click" Margin="0 20" Style="{DynamicResource BaseButtonStyle}"/>
            <Button Content="Зарегистрироваться" Click="Register_Click" Margin="0 10" Style="{DynamicResource BaseButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

