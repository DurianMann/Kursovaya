<Window x:Class="Kursovaya.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursovaya"
        mc:Ignorable="d"
        Title="Регистрация" Height="380" Width="400"
        Style="{DynamicResource WindowStyle}">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Регистрация" Margin="0 10 0 5" Style="{DynamicResource BigCaseStyle}"/>
            <TextBlock Text="Логин:" Margin="0 10 0 0" Style="{DynamicResource LoginStyle}"/>
            <TextBox x:Name="txtRegisterUsername" Margin="0 0 0 10" Style="{DynamicResource BaseTextBoxStyle}"/>

            <TextBlock Text="Пароль:" Margin="0 10 0 0" Style="{DynamicResource LoginStyle}"/>
            <PasswordBox x:Name="txtRegisterPassword" Margin="0 0 0 10" Style="{DynamicResource BasePasswordBoxStyle}"/>

            <Button Content="Зарегистрироваться" Click="Register_Click" Margin="0 20" Style="{DynamicResource BaseButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

